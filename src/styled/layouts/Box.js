import styled from "styled-components";
import { GRAY1 } from "../utilities";

export default styled.div`
  background-color: ${GRAY1};
  padding: 1.5rem;
  border-radius: 3px;

  .heading {
    text-transform: uppercase;
    opacity: 0.7;
    margin-bottom: 0.3em;
    font-weight: 400;
    font-size: 1rem;
    margin: 0;
  }
`;
